version: 1
id: invariantlabs-ai-mcp-scan
name: MCP Scan
status: active
repo: https://github.com/invariantlabs-ai/mcp-scan
compatibility:
  claude-code: check
  codex: check
  cursor: check
  github-copilot: check
  gemini-cli: check
  openclaw: check
  opencode: check
  windsurf: check
allowed_types:
  - mcp-server
discovery:
  manifests: true
  conventions: true
  regex: true
  path_patterns:
    - ".mcp.json"
    - "**/.mcp.json"
  path_regex:
    - pattern: "^\\.mcp\\.json$"
      type: mcp-server
    - pattern: "^(?:.+/)?\\.mcp\\.json$"
      type: mcp-server
artifacts:
  - name: MCP Scan
    type: mcp-server
    path: README.md
exclusions:
  paths: []
  artifacts: []
metadata:
  notes: "MCP security tooling repository discovered from GitHub search."
